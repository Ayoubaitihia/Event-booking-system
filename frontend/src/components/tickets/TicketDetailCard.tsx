import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog"

export default function TicketDetailCard()
{
    return (
        <DialogContent>
            <DialogTitle>Title</DialogTitle>
        </DialogContent>
    )
}